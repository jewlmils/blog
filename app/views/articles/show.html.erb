<p style="color: green"><%= notice %></p>

<h2><%= @article.title %></h2>
<h3>by: <%= @article.writer_name %></h3>
<p><%= @article.body %></p>

<div>
    <%= link_to 'Edit', edit_article_path(@article) %> | 
    <%= link_to 'Back', articles_path %> |
    <%= button_to 'Destroy', @article, method: :delete, data: {turbo_confirm: "Are you sure?"}%>
</div>


<h2>Comments</h2>
<%= render @article.comments %>

<h2>Add a comment</h2>
<%= render 'comments/form', article: @article %>
